# Class encapsulating Daisy log file (dlf) data

Class encapsulating Daisy log file (dlf) data

## Slots

- `header`:

  A list with named components containing meta information

- `units`:

  A data.frame with the units of the values in data

- `data`:

  A data.frame with the values from the dlf file
